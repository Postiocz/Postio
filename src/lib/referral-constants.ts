// Shared referral constants with no server-only dependencies, so they can be
// imported from both Server Components (referral attribution) and Client
// Components (referral-code capture on the login page).
export const REFERRAL_COOKIE = "postio_ref";
